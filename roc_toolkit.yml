---
 depacketizer:
   regex: 'd,\s*(?P<ts>[\d.]*),\s*(?P<missing_samples>[\d.]*),\s*(?P<late_samples>[\d.]*),\s*(?P<recovered_samples>[\d.]*)'
   plots:
     late_samples:
       axis: 1
     missing_samples:
       axis: 1
     recovered_samples:
       axis: 2